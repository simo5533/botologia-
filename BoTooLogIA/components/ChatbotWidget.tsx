"use client";

/**
 * Assistant officiel BoTooLogIA — Bot guide vocal et écrit.
 * Widget flottant : chat texte, reconnaissance vocale (STT), synthèse vocale (TTS),
 * base de connaissances locale, persistance localStorage.
 * Voir components/bot/ pour les sous-composants.
 */
export { BotWidget as ChatbotWidget } from "@/components/bot/BotWidget";

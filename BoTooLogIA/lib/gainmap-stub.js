/**
 * Stub pour @monogrid/gainmap-js lorsque seul Grid de drei est utilisé.
 * Évite l'erreur de résolution au build. Les vrais loaders ne sont pas utilisés par notre code.
 */
module.exports = {
  HDRJPGLoader: function () {},
  GainMapLoader: function () {},
};
